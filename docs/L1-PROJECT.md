# L1 - 项目架构文档

## 项目概述
AI Web Partner Training - 基于 Vite + React + TailwindCSS v4 构建的 AI 对话平台

集成了 DeepSeek AI API，提供多角色 AI 对话功能，采用设计系统驱动的全栈开发架构。

## 技术栈

### 前端技术
- **构建工具**: Vite 5.x
- **框架**: React 18.3
- **路由**: React Router 7.x
- **样式**: TailwindCSS v4 (Vite Plugin)
- **动效**: Framer Motion
- **图标**: Lucide React + React Icons
- **工具**: clsx, tailwind-merge, class-variance-authority
- **Markdown**: react-markdown + remark-gfm

### 后端技术
- **运行环境**: Node.js (ES Module)
- **Web 框架**: Express 4.x
- **数据库**: better-sqlite3
- **AI 服务**: DeepSeek API
- **跨域**: CORS
- **环境变量**: dotenv
- **并发管理**: concurrently

## 架构原则
- 前后端分离架构
- 组件化设计
- 响应式布局
- 性能优先
- **设计系统驱动：一切设计必须来自设计系统**
- RESTful API 设计
- SSE 流式通信

## 🎨 设计系统约束（核心原则）

**重要：整个应用的所有视觉设计必须严格遵循设计系统规范。**

### 必须遵守的规则

1. **颜色来源**
   - 所有颜色必须来自设计系统调色板
   - 禁止使用任意颜色值（如 #FF5733）
   - 使用 TailwindCSS 预定义颜色类（slate, violet, fuchsia 等）

2. **组件来源**
   - 所有 UI 组件必须使用 `/components/ui/` 中的设计系统组件
   - 禁止创建未经设计系统定义的样式变体
   - 新组件必须先在设计系统中定义

3. **间距和尺寸**
   - 使用 TailwindCSS 标准间距系统（0.5, 1, 2, 4, 6, 8, 12, 16...）
   - 禁止使用任意值（如 `w-[237px]`）

4. **排版**
   - 遵循设计系统定义的字体大小和行高
   - 使用语义化的文本样式类

### 设计系统优先级

```
设计系统组件 > 自定义组件
系统颜色 > 任意颜色
标准间距 > 任意间距
```

## 目录结构

### 前端结构
```
/src
  /components
    /ui          # L3 设计系统组件（Button, Card）
    /chat        # L2 聊天模块组件
      ChatSidebar.jsx
      ChatHeader.jsx
      MessageList.jsx
      ChatInput.jsx
      TypingIndicator.jsx
      ScrollToBottomButton.jsx
    Header.jsx   # L2 全局头部
    Hero.jsx     # L2 首页展示
    Footer.jsx   # L2 全局页脚
  /pages         # L2 页面组件
    Home.jsx           # 首页
    ChatPage.jsx       # AI 对话页（全屏独立布局）
    DesignSystem.jsx   # 设计系统展示页
  /layouts       # L2 布局组件
    MainLayout.jsx     # 主布局（Header + Outlet + Footer）
  /lib           # L3 工具库
    api.js       # API 调用封装
    utils.js     # cn() 工具函数
  /assets        # 静态资源
  App.jsx        # 路由配置
  main.jsx       # 应用入口
```

### 后端结构
```
/server
  index.js       # Express 服务器入口
  database.js    # SQLite 数据库操作
  roles.js       # AI 角色配置（系统提示词）
  seed.js        # 数据库初始化脚本
  reset-db.js    # 数据库重置脚本
  /db            # SQLite 数据文件目录
```

## 核心功能架构

### 1. AI 对话系统
- **多角色支持**: 一日三餐顾问、角色扮演模型、通用大模型
- **流式响应**: SSE (Server-Sent Events) 实现打字机效果
- **对话管理**: 创建、切换、删除对话
- **历史记录**: SQLite 持久化存储
- **智能标题**: AI 自动生成对话标题

### 2. 数据流转
```
用户输入 → ChatInput → API → Express → DeepSeek API
                                    ↓
                              SSE Stream
                                    ↓
         MessageList ← ChatPage ← EventSource
```

### 3. 路由架构
- `/` - 首页（Hero + 特性展示）
- `/chat` - AI 对话页（全屏独立布局，不使用 MainLayout）
- `/design-system` - 设计系统文档

## 开发流程
1. L1 定义架构和规范
2. L2 实现功能模块（页面、业务逻辑）
3. L3 构建原子组件（UI 组件、工具函数）

## 分型结构说明
- **L1**: 系统级架构、全局配置、核心理念、技术选型
- **L2**: 模块级功能、页面布局、业务逻辑、API 集成
- **L3**: 组件级实现、工具函数、细节处理、样式变体
