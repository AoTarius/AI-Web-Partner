# 🚀 项目启动说明

## 快速启动（一键启动）

```bash
npm run dev
```

这会同时启动：
- ✅ **前端** (Vite): http://localhost:5173
- ✅ **后端** (Express): http://localhost:3001

## 分别启动

### 方式1：只启动前端
```bash
npm run dev:client
```

### 方式2：只启动后端
```bash
npm run dev:server
```

### 方式3：手动启动（调试用）
```bash
# 终端1：启动后端
node server/index.js

# 终端2：启动前端
npm run dev:client
```

## 快速生成默认对话数据
```bash
npm run seed
```

## 访问地址

- **主页**: http://localhost:5173
- **AI对话页面**: http://localhost:5173/chat
- **设计系统**: http://localhost:5173/design-system
- **后端健康检查**: http://localhost:3001/api/health

## 验证安装

启动后应该看到：

```
✅ 数据库初始化完成
🚀 后端服务器运行在 http://localhost:3001
📊 API 文档: http://localhost:3001/api/health
```

和

```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

## 功能测试

1. 访问 http://localhost:5173/chat
2. 自动创建第一个对话
3. 输入消息并发送
4. 点击"新对话"按钮
5. 在侧边栏切换不同对话
6. 刷新页面，数据仍然保留

## 常见问题

### Q: 端口被占用怎么办？

**前端端口冲突**（5173）：
修改 `vite.config.js`:
```js
server: {
  port: 5174,  // 改成其他端口
  // ...
}
```

**后端端口冲突**（3001）：
修改 `server/index.js`:
```js
const PORT = 3002  // 改成其他端口
```

然后同步修改 `vite.config.js` 的代理配置:
```js
proxy: {
  '/api': {
    target: 'http://localhost:3002',  // 改成新端口
    // ...
  }
}
```

### Q: 如何停止服务？

按 `Ctrl + C` 停止

### Q: 数据库在哪里？

`/server/db/chat.db`

使用 SQLite 客户端可以打开查看：
- [DB Browser for SQLite](https://sqlitebrowser.org/) (推荐)
- [DBeaver](https://dbeaver.io/)

### Q: 如何重置数据库？

```bash
rm server/db/chat.db
npm run dev
```

### Q: 依赖安装失败？

网络问题可能导致安装超时，重试：

```bash
npm install
```

或使用淘宝镜像：

```bash
npm install --registry=https://registry.npmmirror.com
```

## 技术栈

**前端**:
- React 18.3
- Vite 5.x
- TailwindCSS v4
- Framer Motion
- React Router 7

**后端**:
- Node.js 18+
- Express 4.x
- better-sqlite3
- CORS

## 下一步

- [ ] 集成真实 AI API (OpenAI / Claude / 其他)
- [ ] 添加用户认证
- [ ] 消息 Markdown 渲染
- [ ] 代码高亮
- [ ] 导出对话功能
- [ ] 暗黑模式切换
